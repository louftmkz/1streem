# Kopiere diese Datei zu .env.local (lokal) oder setze die Variablen in Vercel → Settings → Environment Variables.
# Im Mock-Modus (src/components/MusicDashboard.jsx → MOCK_MODE = true) werden sie nicht benötigt.

VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_REDIRECT_URI=https://1streem.vercel.app/callback
VITE_BACKEND_URL=
